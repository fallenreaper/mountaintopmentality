{
	"polygon": {
		"token": "",
		"callsPerMinute": 5,
		"definitions": {
			"ticker": "Stock Symbol",
			"multiplier": "A numeric reference.  1 [day], 15 [day], 5 [minute], 15 [minute] to chunk by.",
			"timespan": "String to define bars betters: minute|hour|day|week|month|quarter",
			"startDate": "format: yyyy-mm-dd",
			"endDate": "format: yyyy-mm-dd",
			"sort":"String: asc, desc",
			"limit": "Default 5k, Max: 50k.",
			"token": "Your API Token"
		},
		"routes": {
			"barAggregate": "https://api.polygon.io/v2/aggs/ticker/{ticker}/range/{multiplier}/{timespan}/{startDate}/{endDate}?sort={sort}&limit={limit}&apiKey={token}"		}
	}
}
